export default AiControl = (entities, { touches }) => {
    // From this we can calculate the position of each object for the ant
    // console.log(Object.values(entities).map(val => val?.body?.position))
    
    return entities;
}