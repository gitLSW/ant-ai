import Matter from "matter-js"


// Always the 5 closest Collsions
export default async function aiUpdate(ant, fovCollisions) {
    console.log(ant, fovCollisions)

    // async function calcSightVectors() {
    //     Object.entries(entities)
    //         .filter(entity => entity[0].startsWith('Ant'))
    //         .map(entry => entry[1])
    //         .forEach(calcSightVector)
    // }

    // function calcSightVector(ant) {
    //     Matter.Detector.create({ bodies:  })
    // }

    // calcSightVectors()

    return entities;
}