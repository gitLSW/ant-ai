export default (entities, { touches }) => {
    // From this we can calculate the position of each object for the ant
    // console.log(Object.values(entities).map(val => val?.body?.position))
    
    // const ants = Object.entries(entities).filter(entity => entity[0].startsWith('Ant'))

    // for (const ant of ants) {
    //     const sightVector = 
    // }

    return entities;
}